---

# For use with pre-commit.
# See usage instructions at http://pre-commit.com

  - id: salt-lint
    name: Salt Lint
    description: Lint Salt files
    entry: salt-lint
    language: ruby
    files: \.(sls)$